Remove the LeadConnector chat widget script from `index.html`.

The widget was added via a `<script>` tag at the bottom of the `<body>` in `index.html`. Removing this single tag will stop the widget from loading on every page.

No other code changes are needed — the widget will be gone site-wide once the script is removed.